# Frontend (Nuxt 3)

- Location: `frontend/`
- Configure API base in `frontend/.env`:
  - NUXT_PUBLIC_API_BASE=http://localhost/api
- Allow origin in Laravel `.env`:
  - FRONTEND_URL=http://localhost:3000

Dev:

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:3000
